// if u use localization change it accordingly
import 'package:flutter/material.dart';

const kEmptyFieldErrorMessage = 'هذا الحقل فارغ';
const kPrimaryColor = Color(0xff162840);
const kSecondaryColor = Color(0xff697F97);
const kLinearGradient = LinearGradient(
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    colors: [
      kPrimaryColor,
      Color(0xff3E4963),
      Color(0xff6B6D89),
    ]);

const kDuration = Duration(milliseconds: 2500);

const kTransionDuration = Duration(milliseconds: 500);

const kUserCollection = 'users';
const kUserName = 'userName';

const kEmail = 'userEmail';
